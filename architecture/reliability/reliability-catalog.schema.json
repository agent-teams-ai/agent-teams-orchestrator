{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-teams.ai/schemas/reliability-catalog.schema.json",
  "title": "Agent Teams Orchestrator Reliability Catalog",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "qualificationFramework",
    "profiles",
    "capabilities",
    "metricAttributes",
    "prohibitedMetricAttributes",
    "indicators",
    "invariants",
    "resourceBudgets"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "qualificationFramework": {
      "$ref": "#/$defs/qualificationFramework"
    },
    "profiles": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "$ref": "#/$defs/profile"
      }
    },
    "capabilities": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "$ref": "#/$defs/deploymentCapability"
      }
    },
    "metricAttributes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/metricAttribute"
      }
    },
    "prohibitedMetricAttributes": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/dottedId"
      }
    },
    "indicators": {
      "type": "array",
      "maxItems": 5,
      "items": {
        "$ref": "#/$defs/indicator"
      }
    },
    "invariants": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/invariant"
      }
    },
    "resourceBudgets": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/resourceBudget"
      }
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$"
    },
    "dottedId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:\\.[a-z0-9]+)+$"
    },
    "nonEmptyText": {
      "type": "string",
      "minLength": 12
    },
    "owner": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*(?:/[a-z][a-z0-9-]*)?$"
    },
    "profile": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "description",
        "releaseScope",
        "qualification",
        "blockedBy",
        "productAuthorityAdapter",
        "commercialAuthorityAdapter",
        "requiredCapabilities",
        "qualificationEvidence"
      ],
      "properties": {
        "id": {
          "enum": [
            "managed-saas",
            "standalone-self-hosted",
            "connected-self-hosted",
            "fully-local"
          ]
        },
        "description": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "releaseScope": {
          "enum": ["v1-target", "future"]
        },
        "qualification": {
          "enum": ["blocked", "deferred", "qualified"]
        },
        "blockedBy": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(ADR-[0-9]{4}|OD-[0-9]{3})$"
          }
        },
        "productAuthorityAdapter": {
          "enum": ["managed-platform", "standalone", "local-standalone"]
        },
        "commercialAuthorityAdapter": {
          "enum": ["managed-platform", "optional-managed-platform", "none"]
        },
        "requiredCapabilities": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$"
          }
        },
        "qualificationEvidence": {
          "$ref": "#/$defs/qualificationEvidence"
        }
      }
    },
    "qualificationFramework": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "qualification", "blockedBy", "qualificationEvidence"],
      "properties": {
        "id": {
          "const": "deployment-qualification"
        },
        "qualification": {
          "enum": ["blocked", "qualified"]
        },
        "blockedBy": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(ADR-[0-9]{4}|OD-[0-9]{3})$"
          }
        },
        "qualificationEvidence": {
          "$ref": "#/$defs/qualificationEvidence"
        }
      }
    },
    "deploymentCapability": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "description",
        "qualification",
        "profiles",
        "blockedBy",
        "qualificationEvidence"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$"
        },
        "description": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "qualification": {
          "enum": ["blocked", "deferred", "qualified"]
        },
        "profiles": {
          "$ref": "#/$defs/profileRefs"
        },
        "blockedBy": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(ADR-[0-9]{4}|OD-[0-9]{3})$"
          }
        },
        "qualificationEvidence": {
          "$ref": "#/$defs/qualificationEvidence"
        }
      }
    },
    "qualificationEvidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decisionRefs", "conformanceRefs"],
      "properties": {
        "decisionRefs": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(ADR-[0-9]{4}|OD-[0-9]{3})$"
          }
        },
        "conformanceRefs": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^architecture/qualification-evidence/[a-z0-9][a-z0-9./-]*\\.(json|yaml)$"
          }
        }
      }
    },
    "metricAttribute": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "cardinality", "maxDistinct"],
      "properties": {
        "id": {
          "$ref": "#/$defs/dottedId"
        },
        "cardinality": {
          "enum": ["low", "bounded"]
        },
        "maxDistinct": {
          "type": "integer",
          "minimum": 1,
          "maximum": 64
        }
      }
    },
    "profileRefs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "enum": [
          "managed-saas",
          "standalone-self-hosted",
          "connected-self-hosted",
          "fully-local"
        ]
      }
    },
    "attributeRefs": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/dottedId"
      }
    },
    "objective": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "targetRatio",
        "window",
        "approvedBy",
        "approvedAt",
        "reviewAfter",
        "errorBudgetPolicyRef"
      ],
      "properties": {
        "targetRatio": {
          "type": "string",
          "pattern": "^0(?:\\.\\d+)?$"
        },
        "goodThreshold": {
          "type": "object",
          "additionalProperties": false,
          "required": ["operator", "value", "unit"],
          "properties": {
            "operator": {
              "enum": ["lt", "lte"]
            },
            "value": {
              "type": "string",
              "pattern": "^[0-9]+(?:\\.[0-9]+)?$"
            },
            "unit": {
              "enum": ["milliseconds", "seconds", "bytes", "count"]
            }
          }
        },
        "window": {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "duration"],
          "properties": {
            "kind": {
              "enum": ["rolling", "calendar"]
            },
            "duration": {
              "type": "string",
              "pattern": "^P(?:[1-9][0-9]*D|T[1-9][0-9]*H)$"
            }
          }
        },
        "approvedBy": {
          "type": "string",
          "minLength": 1
        },
        "approvedAt": {
          "type": "string",
          "format": "date"
        },
        "reviewAfter": {
          "type": "string",
          "format": "date"
        },
        "errorBudgetPolicyRef": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "indicator": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "owner",
        "criticalUserJourney",
        "specification",
        "status",
        "measure",
        "unit",
        "source",
        "profiles",
        "allowedAttributes"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "owner": {
          "$ref": "#/$defs/owner"
        },
        "criticalUserJourney": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "specification": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "status": {
          "enum": [
            "candidate",
            "calibrating",
            "aspirational",
            "active",
            "retired"
          ]
        },
        "measure": {
          "enum": ["ratio", "histogram", "gauge"]
        },
        "unit": {
          "enum": ["ratio", "milliseconds", "seconds", "bytes", "count"]
        },
        "source": {
          "$ref": "#/$defs/id"
        },
        "profiles": {
          "$ref": "#/$defs/profileRefs"
        },
        "allowedAttributes": {
          "$ref": "#/$defs/attributeRefs"
        },
        "objective": {
          "$ref": "#/$defs/objective"
        }
      }
    },
    "invariant": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "owner", "statement", "verification"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "owner": {
          "$ref": "#/$defs/owner"
        },
        "statement": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "verification": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "architecture-conformance",
              "contract-conformance",
              "fault-injection",
              "persistence-conformance",
              "property-test",
              "security-fixture"
            ]
          }
        }
      }
    },
    "resourceBudget": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "owner",
        "concern",
        "status",
        "measure",
        "unit",
        "source",
        "profiles",
        "allowedAttributes"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "owner": {
          "$ref": "#/$defs/owner"
        },
        "concern": {
          "$ref": "#/$defs/nonEmptyText"
        },
        "status": {
          "enum": ["candidate", "calibrating", "active", "retired"]
        },
        "measure": {
          "enum": ["histogram", "gauge"]
        },
        "unit": {
          "enum": ["milliseconds", "seconds", "bytes", "count", "ratio"]
        },
        "source": {
          "$ref": "#/$defs/id"
        },
        "profiles": {
          "$ref": "#/$defs/profileRefs"
        },
        "allowedAttributes": {
          "$ref": "#/$defs/attributeRefs"
        },
        "limit": {
          "type": "string",
          "pattern": "^[0-9]+(?:\\.[0-9]+)?$"
        }
      }
    }
  }
}
